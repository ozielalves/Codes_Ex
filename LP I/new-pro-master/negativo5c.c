#include <stdio.h>

int v[4];
int main ( void)
{
	scanf ( "%i, &v" );
	int count_neg;
	for ( i = 1; i = 4; i++)
	{

		v[i];

		if ( i<0 ) 
		{

			count_neg ++;

		}

	}
	printf( "%i\n", count_neg );
	return 0;
}