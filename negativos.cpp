#include <iostream>
int main( void )
{
	int v[SIZE]
	stdout << "Forneca 5 elementos interos. \n";
	std::cin >> v
	auto count_neg(0); //contando os negativos
	for( auto i(0); i<SIZE; ++i )
	{
		auto temp(0)
		std::cin >> temp;
		if ( temp < 0 > count++;
	}

	std::cout << ">>> # de negativos: " << cout << "\n";

	return 0;
}
